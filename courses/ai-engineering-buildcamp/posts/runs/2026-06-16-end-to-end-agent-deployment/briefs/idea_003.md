# Handoff Brief: Use one Docker image for a small agent app

Source: /Users/valeria/.codex/content-runs/2026-06-16-end-to-end-agent-deployment/source/transcript.json

## 01:18:13 - 01:21:32

>> Okay. Um so um now what I want to do like of okay this is not ideal as I mentioned like we are not actually continuing conversation we are starting conversation from scratch every time we send the request. So there is no continuation. So this is something we just keep in mind that this is something we will improve later in the next versions. So what I want to do now is I want to package this inside a docker container right and I want to have one docker container. I want this docker container to serve both front end and back end and u then we will deploy this container. So this is what I will say. Uh okay right now everything works. So I want to deploy it and for that I want to create a docker container. This docker container should serve both back end and front end. So uh when we build a docker container so it should build our front end application it should package this and it should insert it inside our front end uh container. So then the sorry backend container so the backend container then will serve uh the built JavaScript. Okay. So we want to have only one docker container. We don't want to have two separate containers. We don't have a container for back end and front end. one is enough for us. Uh but typically when you have this um when you develop you have one um server service running for bit another service is running for back end. So kind of it's like two things but instead of that we only want to have one. So I'll stop this bit for now. I think I will also stop this thing. Yeah. Yeah. So typically in order to avoid that what uh people do is they that there is this build step when um this JavaScript project is turned into u like an actual files that would be served with uh like a browser can read right and then we serve these files through the back end. So back end is serving both front end and is making goals and I think we saw here um past API like CL code add in this static directory. So this is actually what it's uh going to use for serving this stuff. So now it's let me check docker image. Okay. So it's doing it in two stages. So the first stage is building uh back end uh building front end sorry and the second stage is building back end which makes total sense uh this is how I actually wanted to do um so then like we don't really need to have NodeJS dependencies in our uh back end right so this is already built um it has all the files and we just copy these files here from front end build we put them into this app static and then our uh back end our um how do we call it like fast API will just simply serve it okay seems reasonable I don't have any um

## 01:23:24 - 01:26:29

>> Hi, Alex. I was just wondering about the Docker step that you said basically that we have we don't want to have any NodeJS dependencies in the back end. Um, how did we actually ensure that the back end starts running once the front end is done if we don't even have uh a compose file? >> Yeah. Um, so uh we don't need to do this. We don't have a compost file here. So we have the first stage. So let me do docker uh build actually docker build minus t. How do you call it? and to end workshop. So right now it's doing ah we already did this. So you see first it's running stage number one. It's building stage number one. So it builds. Okay. Sorry I'm confused. Yeah. Front end builder. I'm just trying to understand the order. Anyways um so what is happening is first it builds this. So this is stage number one. First it builds this and only after this thing is finished it builds this. Right? So we don't have compose we don't have two containers running at the same time. This is happening at the build time. So I'm not serving anything yet. So I first I built this but as a part of this I built first stage number one and then stage number two and when I built stage number one uh the result of this thing is a bunch of JavaScript files or maybe just one JavaScript file. So we have this inside this app.ist here. So when I run npm build, npm run build, it it creates this uh uh slash app dist folder, right? And then this folder is copied into static, right? So these are two separate steps. And when we run compost, compose runs only this. So here the front end is already built. So we don't that's why we don't need to think uh so don't think about this as two separate containers think about this as first we run this we build these things so now they are in a folder and we just take this folder and copy it to our uh backend container does make sense >> yeah totally thanks first time I see it but it's cool thanks >> it is cool yeah because it allows us to actually not have NodeJS dependency here cuz otherwise we would need to have uh install Node.js here, build it and then use it, right? But here we create our throwaway container, right? We build it there and okay, we only need one part there, we can throw away the rest. It's very it's very nice pattern or I say

## 01:28:23 - 01:30:04

So the the problem is that uh um it couldn't get the API key from the apparently. So, incorrect key. Um, uh, the key actually works cuz we were able to to uh do this before, but when I run it inside Docker, it couldn't. So, I think the problem is with how we propagate the key from our uh from our host machine to Docker to the image. I should have saved to the container, but it doesn't matter. Okay. I had no idea that Docker parses it differently and we actually have uh quotes. I have no idea. I had no idea about that. Okay, so it removed quotes. Okay. So, let me Oops. So, made docker build. Now, my make docker run. Actually, I didn't even need to rebuild it, right? Cuz um and the end file changed. Nothing inside changed. I join the course.

## Post angle

For a small agent application, one Docker image can serve the built frontend and the FastAPI backend. A multi-stage build keeps Node.js out of the runtime image.

## Clip recommendation

Use 01:23:24 - 01:26:29.

## Writing brief

Write a practical post explaining when one container is enough for a small agent app, how a multi-stage Docker build works, and why environment variables should be tested inside the container.
