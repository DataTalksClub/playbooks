# Week Brief

Save a filled copy as `newsletter/issues/<folder>/brief.md`. Zoomcamps, events, Book of the Week, recordings, and Alexey on Data come from `sources.md`; ask the user only for the fields below that neither `sources.md` nor `newsletter/sponsors.yaml` answers. Write `none` for a slot that is skipped this week.

```markdown
Send date:

Primary promo:
- Sponsor or product:
- Paid placement (adds "Sponsored content"): yes / no
- Copy or source (sponsor text, landing page, doc):
- Button label and URL:
- Promo code or key date:
- Subject line and preview text supplied by the sponsor, if any:

Secondary promo:
- Sponsor or product:
- Paid placement: yes / no
- Copy or source:
- Button label and URL:

Running Zoomcamps to leave out this week, if any:

Course registration slots (courses open for registration before their cohort starts):

Events that get a detail slot:

Alexey on Data post to feature (if several):

Recording to feature (if several):

Anything else this week:
```
